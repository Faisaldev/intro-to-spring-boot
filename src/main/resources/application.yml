spring:
  application:
    name: intro-to-spring-boot
  output:
    ansi:
      enabled: always
server:
  port: 0
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
